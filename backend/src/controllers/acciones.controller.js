import { successResponse } from '../utils/response.utils.js';

export const listAcciones = async (_req, res) => {
  return successResponse(res, [], 'Listado de acciones (pendiente de implementación)');
};
