import { successResponse } from '../utils/response.utils.js';

export const listEntradas = async (_req, res) => {
  return successResponse(res, [], 'Listado de pedidos de entradas (pendiente de implementación)');
};
