import { successResponse } from '../utils/response.utils.js';

export const listIntegrantes = async (_req, res) => {
  return successResponse(res, [], 'Listado de integrantes (pendiente de implementación)');
};
